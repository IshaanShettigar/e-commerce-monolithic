// Make calls to the database to fetch cart details

// Build the logic to update cart item subtotal based on quantity
// const cartRows = document.getElementsByClassName("cart-row")
// for (var i = 0; i < cartRows.length; i++) {
//     const cartRow = cartRows[i]
//     const quantity = cartRow.getElementsByClassName("qty")[0];
//     const basePrice = cartRow.getElementsByClassName("base-price")[0];
//     const finalPrice = cartRow.getElementsByClassName("final-price")[0];
//     console.log(quantity, basePrice, finalPrice.innerHTML)
//     finalPrice.innerHTML = basePrice.innerHTML * quantity.innerHTML + 1

// }
// Build the logic to update cart total